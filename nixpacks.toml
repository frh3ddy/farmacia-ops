[phases.setup]
nixPkgs = ["nodejs_20", "npm"]

[phases.install]
cmds = ["NODE_ENV= npm install"]

[phases.build]
cmds = [
  "npm run prisma:generate",
  "npm run build"
]

# Migrations run at startup to ensure DATABASE_URL is available
# This is handled in apps/api/src/index.ts bootstrap function

