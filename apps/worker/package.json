{
  "name": "@farmacia-ops/worker",
  "version": "1.0.0",
  "scripts": {
    "build": "node -e \"const {execSync}=require('child_process');const fs=require('fs');const path=require('path');const tsc=require.resolve('typescript/bin/tsc');const pkgDir=path.dirname(require.resolve('./package.json'));const distDir=path.join(pkgDir,'dist');if(!fs.existsSync(distDir)){fs.mkdirSync(distDir,{recursive:true});}process.chdir(pkgDir);try{execSync('node '+tsc,{stdio:'inherit'});const output=path.join(distDir,'worker.js');if(!fs.existsSync(output)){console.error('Build failed: dist/worker.js was not created');process.exit(1);}}catch(e){console.error('TypeScript compilation failed');process.exit(1);}\"",
    "start": "node -e \"const fs=require('fs');const path=require('path');const pkgDir=path.dirname(require.resolve('./package.json'));const dist1=path.join(pkgDir,'dist/worker.js');const dist2=path.join(pkgDir,'dist/src/worker.js');const file=fs.existsSync(dist1)?dist1:fs.existsSync(dist2)?dist2:null;if(!file){console.error('Error: dist/worker.js not found. Build may have failed.');process.exit(1);}require(file);\"",
    "test:connection": "ts-node src/test-connection.ts"
  },
  "dependencies": {
    "@farmacia-ops/shared": "file:../shared",
    "bullmq": "^5.0.0",
    "ioredis": "^5.3.2"
  },
  "devDependencies": {
    "@types/node": "^25.0.3",
    "ts-node": "^10.9.2",
    "typescript": "^5.0.0"
  }
}